# 2026-07-17 工作日志

## 重新安装 Android SDK（D盘）

- 用户要求重新下载 SDK 到 D 盘
- 下载 `commandlinetools-win-14742923_latest.zip`（144 MB）到 `D:\AndroidSDK`
- 解压并重组目录结构：`D:\AndroidSDK\cmdline-tools\latest\`（符合 sdkmanager 要求）
- 设置环境变量 `ANDROID_HOME=D:\AndroidSDK`，`JAVA_HOME=C:\Program Files\Java\jdk-21.0.11`

## 安装 SDK 组件

- 使用 `sdkmanager --licenses` 接受所有许可证
- 安装 `build-tools;36.0.0`、`platforms;android-36`、`platform-tools`
- 注意：`--sdk_root` 参数在 Git Bash 下行为异常，直接使用 `ANDROID_HOME` 环境变量更可靠
- `--list_installed` 在 `ANDROID_HOME` 指向新目录时显示为空，安装后才正确显示

## 修复 @capacitor/app 构建错误

- Gradle 构建失败：`getDefaultProguardFile('proguard-android.txt')` is no longer supported
- 原因：`@capacitor/app@8.0.0` 使用了已弃用的 ProGuard 配置，与 AGP 9.x 不兼容
- 解决：升级 `@capacitor/app` 到 `8.1.1`，该版本已修复为使用 `proguard-android-optimize.txt`

## 第三次打包 APK（成功）

- 用户再次请求重试打包
- dist 已更新（15:14），APK 已过期（14:39）
- `npm run build` → web 资源构建成功
- `npx cap sync android` → 成功（检测到 2 个插件：`@capacitor-community/sqlite@8.1.0` 和 `@capacitor/app@8.1.1`）
- Gradle 构建：`BUILD SUCCESSFUL in 25s`（151 个任务，26 执行，125 已是最新）
- **新 APK 生成**：
  - 路径：`android/app/build/outputs/apk/debug/app-debug.apk`
  - 构建时间：15:22
  - 大小：28 MB
